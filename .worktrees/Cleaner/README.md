# sim-server

Project created by SwarmForge-2.0.

## Start

1. Install the project in editable mode:

   ```cmd
   python -m pip install -e .
   ```

2. Run the command-line entry point:

   ```cmd
   sim-server --help
   ```

3. Run the tests:

   ```cmd
   python -m pytest -q
   ```
